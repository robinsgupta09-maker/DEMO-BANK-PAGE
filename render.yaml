services:
  - type: web
    name: hofc-bank-portal
    env: static
    buildCommand: cd frontend && npm install && npm run build
    publishPath: frontend/build
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
